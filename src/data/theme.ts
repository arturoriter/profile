export interface Theme {
  page: {
    textColor: string;
    backgroundColor: string;
  };
  h1: {
    backgroundColor: string;
    textColor: string;
  };
  header: {
    backgroundColor: string;
    textColor: string;
  };
  footer: {
    backgroundColor: string;
    textColor: string;
  };
  button: {
    backgroundColor: string;
    textColor: string;
  };
  linkedinBadge: string;
  table: string;
}
